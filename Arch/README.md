# Arch Shell

This folder is the Quickshell desktop (taskbar, modules, SDDM theme, GRUB helpers).

On a new machine run **`../install.sh`** from the repo root.

Do not commit Agent Centre keys. They belong in `~/.config/arch-shell/agent/providers.json` on the machine, never in git.
